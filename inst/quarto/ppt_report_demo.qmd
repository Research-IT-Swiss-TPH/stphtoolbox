---
title: "Swiss TPH Powerpoint presentation example"
subtitle: "My example subtitle"
author: "Research IT"
format: pptx
editor: visual
metadata-files:
  - _ppt_metadata.yml
---

# Introduction to Quarto

## Quarto

Quarto enables you to weave together content and executable code into a finished presentation. To learn more about Quarto presentations see <https://quarto.org/docs/presentations/>.

## Bullets

When you click the **Render** button a document will be generated that includes:

-   Content authored with markdown
-   Output from executable code

# Graphics generated by R

## Colors

```{r}
# Load Swiss TPH graphical chart colors
stph_colors <- stphtoolbox::stph_palette()
scales::show_col(stph_colors)
```

## Results

The iris dataset was used for this demo.

```{r}
library(dplyr)
library(ggplot2)
iris %>% 
  ggplot2::ggplot() +
  ggplot2::geom_boxplot(mapping = ggplot2::aes(y    = Sepal.Length,
                                               fill = Species)) +
  ggplot2::scale_fill_manual(values = unlist(stph_colors)) +
  ggplot2::theme_bw() +
  ggplot2::theme(panel.grid.major.x = element_blank(),
                 panel.grid.minor.x = element_blank(),
                 axis.text.x        = element_blank(),
                 axis.ticks.x       = element_blank())
```
