% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{generate_pdf_report}
\alias{generate_pdf_report}
\title{Generate a PDF report using Quarto (Typst or LaTeX)}
\usage{
generate_pdf_report(
  qmd_filename,
  pdf_filename,
  engine = "typst",
  filename_format = "",
  params = list()
)
}
\arguments{
\item{qmd_filename}{Filename of the Quarto document to be processed (with *.qmd extension).
Must already declare the \verb{report-*} format matching \code{engine} (see above).}

\item{pdf_filename}{Filename for the generated PDF report (without *.pdf extension).}

\item{engine}{PDF engine: "typst" (default, no LaTeX needed) or "latex".}

\item{filename_format}{Specify the filename format, options include an empty string (default) or "with_date".}

\item{params}{List of parameters to customize the report generation process (optional, default is an empty list).}
}
\value{
The function does not explicitly return a value but generates a PDF report in the working directory.
}
\description{
Renders a Swiss TPH \code{.qmd} to PDF in the working directory using the Swiss
TPH \code{report} Quarto extension. Two engines are available: Typst (default, no
LaTeX toolchain required) and LaTeX (\code{engine = "latex"}, needs a LaTeX
installation such as TinyTeX). The \code{.qmd} header must declare the \verb{report-*}
format matching the chosen engine (see "Required YAML header"); the function
then copies the bundled \verb{_extensions/report/} extension next to the \code{.qmd}
and renders it. Any bibliography you reference in the \code{.qmd} header is your
own responsibility -- the function does not manage \code{references.bib}.
}
\section{Required YAML header}{

For the default Typst engine the \code{.qmd} must use (see \code{report_simple_demo_typst.qmd}):
\preformatted{
format:
  report-typst:
}
For the LaTeX engine the \code{.qmd} must use (see \code{report_simple_demo.qmd}):
\preformatted{
format:
  report-pdf:
}
}

